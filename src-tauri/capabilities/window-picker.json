{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "window-picker",
  "description": "The one-shot window picker surface (#124). Its own commands are the app's, which need no grant; this covers the core plumbing every webview uses, setting its own window title so the picker announces itself in the app's language, and reading the system locale. Deliberately narrower than the default capability: the picker touches no files, no store, no dialogs, no updater and no shortcuts.",
  "windows": ["window_picker"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "os:allow-locale"
  ]
}
