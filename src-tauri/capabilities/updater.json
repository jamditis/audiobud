{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-updater",
  "description": "Updater access for installed Windows builds. macOS v0.6.0 uses manual downloads.",
  "platforms": ["windows"],
  "windows": ["main"],
  "permissions": ["updater:default"]
}
